# rstl.kernel

this is not necessarily a kernel, but an experiment for creating a tiny kernel, module, and firmware set that can be used by (most) people. inspired by people on gentoo and other platforms creating <20mb driver sets.
